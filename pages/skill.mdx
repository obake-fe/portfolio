---
type: page
title: Skills
date: 2025-08-17
---

import { skills } from './constants/skills'
import {
  Box,
  Button,
  Heading,
  HStack,
  SimpleGrid,
  Text,
  VStack,
  Image
} from '@chakra-ui/react'

<Heading as="h2">FrontEnd</Heading>

<SimpleGrid columns={[1, 2, 3]} spacing={6} gap={2}>
  {skills.map((skill) => (
    <Box
      key={skill.name}
      borderWidth="1px"
      borderRadius="lg"
      overflow="hidden"
      p={4}
      _hover={{ shadow: 'lg', transform: 'scale(1.05)', transition: '0.2s' }}
    >
      <VStack spacing={4}>
        <Image
          src={`/images/icons/${skill.icon}`}
          alt={skill.name}
          boxSize="60px"
        />
        <Text fontSize="xl" fontWeight="bold">
          {skill.name}
        </Text>
        <Text fontSize="sm" color="gray.500">
          {skill.years} 年
        </Text>
        <Text fontSize="sm" textAlign="center">
          {skill.comment}
        </Text>
      </VStack>
    </Box>
  ))}
</SimpleGrid>

<Heading as="h2">Testing & CI</Heading>
